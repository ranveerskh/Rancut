Replace/add these 3 files at the exact paths in your editor repository:
.github/workflows/publish-installer.yml
scripts/set-release-version.cjs
tests/ui-build.test.js

Keep the previous updater patch files too.
Commit all 3 files. Actions > Prepare downloadable installer > Run workflow on main, version 0.6.5.
Start a NEW workflow run; rerunning the old failed run uses its old commit.
After success, test the installer and publish the generated draft release to make it discoverable.

This fix synchronizes package.json, both root lockfile versions, and embedded editor version labels during the build. It preserves dependency versions. The UI test now checks real version consistency instead of requiring 0.6.4 forever.
Verified locally: all 6 ui-build tests pass after setting the release to 0.6.5. Windows workflow still needs to run on GitHub.
