import React,{useEffect,useRef,useState} from 'react';
export default function HelpPanel({onClose,diagnostics}){
 const [feedback,setFeedback]=useState(''),[feedbackType,setFeedbackType]=useState('Feature request');
 const saveFeedback=()=>{const text=`RanCut ${feedbackType}\nVersion: 0.5.0 Preview · Build 50\n\n${feedback.trim()}\n`;const url=URL.createObjectURL(new Blob([text],{type:'text/plain'}));const a=document.createElement('a');a.href=url;a.download='RanCut-feedback.txt';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);};
 const [updateMessage,setUpdateMessage]=useState(''),[updateAvailable,setUpdateAvailable]=useState(false);
 const checkUpdates=async()=>{setUpdateMessage('Checking…');try{if(!window.rancut?.checkUpdates){setUpdateMessage('Update checks are available in the desktop app.');return;}const r=await window.rancut.checkUpdates();setUpdateAvailable(!!r.available);setUpdateMessage(!r.configured?'Release server is not configured yet.':r.available?'Version '+r.latest+' is available.':'You have the latest published version.');}catch(e){setUpdateMessage(e.message);}};
 const dialog=useRef(),[tab,setTab]=useState('Start');
 useEffect(()=>{dialog.current.showModal();return()=>dialog.current?.close();},[]);
 const download=()=>{const url=URL.createObjectURL(new Blob([JSON.stringify({...diagnostics,platform:navigator.platform},null,2)],{type:'application/json'}));const a=document.createElement('a');a.href=url;a.download='RanCut-diagnostics.json';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);};
 return <dialog className="helpDialog" ref={dialog} onCancel={onClose}><header><div><small>LOCAL CREATOR EDITOR</small><h2>RanCut <small>0.5.0 · Preview</small></h2></div><button aria-label="Close help" onClick={onClose}>×</button></header>
 <nav>{['Start','Shortcuts','About','Feedback','Account','Privacy','Licences','Updates'].map(t=><button className={t===tab?'active':''} onClick={()=>setTab(t)} key={t}>{t}</button>)}</nav>
 <section>{tab==='Start'?<><h3>Your first edit</h3><ol><li>Import footage. Put the background on V1 and the person on V2.</li><li>Select the person → Chroma → enable key and sample the green screen.</li><li>Audio → Detect pauses → review before removing.</li><li>Styles → choose the person track → Simple or Dynamic → Apply.</li><li>Click + on a cut for Transitions. Choose this cut / full video and optional sound.</li><li>Export → choose your folder. Keep the lid open and power connected.</li></ol><p>Auto Edit guides source selection, cuts, styles, backgrounds and audio, saving an editable project copy. Click an audio track name for full-track volume. For slow 4K preview, prepare 720p proxies in Media. Exports always use originals. Save a project file as well as autosave.</p></>:tab==='Shortcuts'?<><h3>Keyboard shortcuts</h3><p>Space: play/pause · X: split · Z: crop left · C: trim right · Q/W: ripple left/right · G: close gap · L: link · M: marker · F: fullscreen</p><p>Ctrl+A: select all · Ctrl+S: save · Ctrl+Z: undo · Ctrl+Shift+Z: redo · Shift-click: multi-select · Delete: delete selected clips/gap.</p><p>Check editing scope before trimming: All unlocked tracks or Selected + linked clips.</p></>:tab==='About'?<><img className="aboutIcon" src="./icon.svg" alt="RanCut icon"/><h3>RanCut — built for creators</h3><p>Version 0.5.0 Preview · Build 50</p><p>Local video editing, Creator Styles, chroma key and GPU-capable export.</p><p>Project Home keeps named projects and up to five recovery versions locally. Save project files separately for backup. Original media is never included in project JSON.</p><p>This is a preview release. Windows installer and hardware checks are still required before a public release.</p></>:tab==='Feedback'?<><h3>Request a feature / Report a bug</h3><label>Type<select value={feedbackType} onChange={e=>setFeedbackType(e.target.value)}><option>Feature request</option><option>Bug report</option></select></label><textarea aria-label="Feedback details" placeholder="What were you trying to do? What happened, and what did you expect?" value={feedback} onChange={e=>setFeedback(e.target.value)}/><div className="feedbackActions"><button disabled={!feedback.trim()} onClick={saveFeedback}>Save feedback draft</button></div><p>No support destination is configured yet. This saves a text file for you to share with the developer; it does not send anything. Diagnostics are optional and can be downloaded below.</p></>:tab==='Account'?<><h3>Account & Plan</h3><p>Local preview — no account required.</p><p>All features in this preview remain available. No subscription is active and no payment is collected.</p><p>Paid plans, secure sign-in, payment processing and offline licence checks are not connected in this build.</p></>:tab==='Privacy'?<><h3>Local by design</h3><p>Media, analysis, previews and exports stay on this computer. This build has no accounts, analytics, advertising or automatic cloud uploads.</p><p>Autosave and presets are stored locally. Remembered source paths stay in app data and project files. Only previously imported, unchanged files reconnect automatically. Proxy files are temporary and removed on clean exit.</p><p>Diagnostics are downloaded only when you click the button. They omit source paths, clip names and media content. Review before sharing.</p></>:tab==='Licences'?<><h3>Third-party software</h3><p>React / React DOM — MIT. Vite — MIT. Lucide icons — ISC. Electron and Chromium include multiple third-party licences.</p><p>FFmpeg licensing depends on the binary and its enabled components. The bundled ffmpeg-static package uses GPL-3.0-or-later. Public distribution needs the matching FFmpeg notices/source compliance, dependency notices and your own terms.</p><p>This preview is not a legal clearance or a signed public release. Review THIRD-PARTY-NOTICES.md and PUBLIC-RELEASE-CHECKLIST.md in the source.</p></>:<><h3>Updates</h3><p>Installed version: 0.5.0 (preview).</p><p>Checks run only when requested. A verified release server must be configured before public launch. Installers are never silently downloaded or installed.</p><button onClick={checkUpdates}>Check for updates</button>{updateAvailable&&<button onClick={()=>window.rancut.openRelease().catch(e=>setUpdateMessage(e.message))}>Open release page</button>}<p role="status">{updateMessage}</p><p>Until then, use the new installer from your own successful GitHub build. Keep a backup of saved projects when upgrading.</p></>}</section>
 <footer><button onClick={download}>Download diagnostics</button><button onClick={onClose}>Done</button></footer></dialog>;
}
